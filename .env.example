BASE_URL=http://localhost
PORT=3000
JOB_APP_MANAGER_TYPE=prod
#JOB_APP_MANAGER_TYPE=test

# Browser Extension API Key
# Generate a secure random string for production use
# Default: dev-api-key (for development only)
BROWSER_EXTENSION_API_KEY=dev-api-key
