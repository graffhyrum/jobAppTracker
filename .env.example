# Required
BASE_URL=http://localhost:3000
PORT=3000
JOB_APP_MANAGER_TYPE=prod

# Optional — extension API is disabled when unset (minimum 32 characters when set)
# BROWSER_EXTENSION_API_KEY=your-secret-key-minimum-32-chars-long
