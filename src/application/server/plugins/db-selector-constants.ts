/**
 * Shared constants for the database selector feature.
 * Used across dev tools, middleware, and page rendering.
 */

export const DEV_DB_COOKIE_NAME = "dev-db-selection";
export const COOKIE_MAX_AGE = 60 * 60 * 24 * 7; // 7 days
